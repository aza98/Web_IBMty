name: FTP Uploader
  # You may pin to the exact commit or the version.
  # uses: zinh/ftp-uploader-action@eb1fe764f787e940a61290a634daf7226421f0f4
  uses: zinh/ftp-uploader-action@v0.5
  with:
    # Hostname of ftp server, eg: ftpHostname: ftp.test
    ftpHostname: 0398b5f.netsolhost.com
    # User of ftp account
    ftpUsername: ibmty2020
    # Password of ftp account
    ftpPassword: Faith2God
    # List files to upload to ftp server
    files: 
    # Source folder
    src: # optional, default is 
    # Destination folder
    dest: # optional, default is /
    # For debug
    dryRun: # optional
    # Files to ignore
    ignore: # optional, default is []
          
